class Hello extends React.Component {
    render() {
        return React.createElement(
            'h1',
            null,
            ' Hello world? '
        );
    }
}

ReactDOM.render(React.createElement(Hello, null), document.getElementById('one'));
